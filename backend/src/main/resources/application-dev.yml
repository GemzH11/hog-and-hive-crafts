app:
  cors:
    # CORS: YAML list of allowed origins for the dev profile, with one origin per "-" entry.
    # Leave blank to disable cross-origin access by default; production defaults may come from application.yml or other profiles.
    allowed-origins:
      - "http://localhost:5173"
spring:
  datasource:
      password: <REPLACE ME>
      url: <REPLACE ME>
      username: <REPLACE ME>
  jpa:
    hibernate:
      ddl-auto: create-drop
