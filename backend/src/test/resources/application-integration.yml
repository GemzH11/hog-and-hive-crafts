spring:
  datasource:
    password: ${SPRING_DATASOURCE_PASSWORD:changeme}
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/hogandhivecrafts_db}
    username: ${SPRING_DATASOURCE_USERNAME:user}